class Appointment {
  var time = "12";
  var day = "Monday";
  var name = "Shashank";
  var service = "Charging";
  var status = 'active';
  var id = "1";

  Appointment(
      {required DateTime time,
      required name,
      required service,
      required status,
      required id});
}
